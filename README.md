# BR_IHC4BC_image_conversion

## 📌 개요
**BR_IHC4BC_image_conversion** 리포지터리는 유방암 관련 IHC(면역조직화학) 이미지를 대상으로 한 이미지 변환 실험을 위한 코드와 노트북을 모아둔 저장소입니다. 주로 CycleGAN 기반의 Stain / Domain 변환 실험을 포함하며, 아래 마커별(ER, HER2, PR 등) 변환을 위한 개별 노트북이 포함되어 있습니다.

---

## 🔧 주요 내용
- `DataPreprocessing.ipynb` : 데이터 전처리 및 학습에 적합한 폴더/파일 구조 생성용 노트북
- `CycleGAN_ER.ipynb` : ER 마커에 대한 CycleGAN 실험 노트북
- `CycleGAN_HER2.ipynb` : HER2 마커에 대한 CycleGAN 실험 노트북
- `CycleGAN_PR.ipynb` : PR 마커에 대한 CycleGAN 실험 노트북
- `CycleGAN_membrane.ipynb` : Membrane 관련 변환 실험 노트북
- `CycleGAN_nucleus.ipynb` : Nucleus 관련 변환 실험 노트북
- `figure/` : 실험 결과 시각화(이미지/그래프) 저장 폴더

> ⚠️ 원본 데이터(이미지)는 저장소에 포함되어 있지 않습니다. 사용자는 적절한 데이터셋을 준비하고 아래 권장 구조를 따라야 합니다.

---

## 📁 권장 데이터 구조
다음 예시는 각 마커별로 CycleGAN 학습 시 흔히 사용하는 폴더 구조입니다:

```
data/
  ER/
    trainA/
    trainB/
    testA/
    testB/
  HER2/
    ...
  PR/
  membrane/
  nucleus/
```
- `A`와 `B`는 서로 다른 도메인(예: 서로 다른 염색 방식 혹은 원본↔타깃)을 의미합니다.
- 파일은 일반적으로 이미지(`.png`, `.jpg`) 형식이어야 합니다.

---

## ⚙️ 사용 방법 (간단 가이드)
1. Python 3.8+ 환경에서 Jupyter Notebook을 실행합니다.
2. 필요한 패키지를 설치합니다 (예: `torch`, `torchvision`, `numpy`, `matplotlib` 등). GPU 사용을 권장합니다.
3. `DataPreprocessing.ipynb`를 실행하여 데이터 구조를 맞춥니다.
4. 각 마커의 `CycleGAN_*.ipynb`를 열고 데이터 경로를 지정한 뒤 셀을 순서대로 실행하여 학습 및 결과 확인을 합니다.

> 팁: 모델 학습은 GPU 메모리에 크게 의존합니다. 리소스가 제한적이면 작은 이미지 크기와 배치 크기로 실험해 보세요. ✅

---

## 💡 실험 권장 사항
- 학습/검증 분리, 데이터 증강(augmentation) 적용을 통해 과적합을 방지하세요.
- 체크포인트를 주기적으로 저장하고, 시각화를 통해 결과를 정성적으로 평가하세요.
- 여러 시드로 반복 실험하여 결과의 일관성을 확인하세요.

---

## � 결과 (Results)
아래는 실험 결과를 정리하는 권장 형식과 예시입니다. 결과 이미지는 `figure/` 폴더 아래에 저장하시고, 파일명은 마커별로 분명하게 지정하세요 (권장: `ER_before_after.png`, `ER_metrics.png`, `HER2_before_after.png`, 등).

### ✅ 대표 결과 예시 (템플릿)
- ER
  - 이미지: `figure/ER_before_after.png`
  - 설명: 원본(real) → 생성(fake) 비교, 주요 관찰 포인트(색감 변화, 구조 보존 등)
- HER2
  - 이미지: `figure/HER2_before_after.png`
  - 설명: ...
- PR
  - 이미지: `figure/PR_before_after.png`
  - 설명: ...

#### Membrane (실제 결과)
![Membrane: real → fake](figure/membrane_result.png)
*Figure.* Membrane: 왼쪽은 원본, 오른쪽은 변환 결과. 관찰: 세포막 구조 보존이 양호하고 색감 변화가 자연스럽습니다.

#### Nucleus (실제 결과)
![Nucleus: real → fake](figure/nucleus_result.png)
*Figure.* Nucleus: 왼쪽은 원본, 오른쪽은 변환 결과. 관찰: 핵 구조 보존이 전반적으로 양호하나, 미세 구조 일부에서는 차이가 보일 수 있습니다.


### 📋 결과 요약 템플릿
아래 표를 사용해 마커별 정량적/정성적 결과를 정리하세요. 필요 시 열을 추가해 더 많은 메트릭을 기록할 수 있습니다.

| Marker | Dataset | FID | SSIM | PSNR | Notes |
|---|---:|---:|---:|---:|---|
| ER | test | — | — | — | 색감 보정 우수, 세포 경계 일부 손실 |
| HER2 | test | — | — | — | 세포막 구조 보존 잘됨 |
| PR | test | — | — | — | ... |
| membrane | test | — | — | — | 세포막 구조 보존 우수; 색감 자연스러움 |
| nucleus | test | — | — | — | 핵 구조 보존 양호; 미세구조 일부 손실 가능 |

---

### 🔧 자동 리포트(선택사항)
결과 이미지를 자동으로 수집해 정리/추출하는 스크립트를 만들면 편리합니다. 예시(간단한 뼈대):

```python
# scripts/generate_results_summary.py (예시)
# - 입력: figure 폴더
# - 출력: results_summary.md 또는 CSV
# - 수행: 이미지 파일 스캔, 메트릭 계산 (FID/SSIM/PSNR), 마크다운/CSV로 기록
```

필요하시면 이 스크립트의 템플릿도 만들어 드리겠습니다. ✅

---

## �📝 라이선스 및 기여
- 본 저장소의 소스는 필요에 따라 라이선스 정보를 추가해주세요.
- 기여 환영합니다 — 이슈 작성 또는 풀 리퀘스트(PR)를 통해 개선 제안 부탁드립니다.

---

## 📞 연락
- 궁금한 점이나 개선 제안은 리포지터리 소유자에게 문의하세요.

---

감사합니다! 🎯
